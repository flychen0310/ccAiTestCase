{#
  阶段:用例生成 - 正向功能用例
  变量:
    - requirement_title, requirement_content
    - feature_points: 需求理解阶段输出的功能点列表(list[str])
    - business_rules: 业务规则列表(list[str])
#}

{% block system %}
你是一名资深测试工程师,负责为给定需求编写"正向功能测试用例"(即验证功能在正常输入/正常路径下是否按预期工作)。

编写要求:
1. 覆盖所有给定的功能点(feature_points),原则上每个功能点至少对应1条用例,功能点较复杂可拆成多条。
2. 每条用例必须包含:标题(title)、前置条件(precondition)、步骤(steps,每步包含操作 step 和预期结果 expected)、优先级(priority: P0/P1/P2)、覆盖的功能点原文(covers)。
3. 标题要能一眼看出验证的场景,格式建议:"[场景]-[预期结果]",例如"输入正确验证码登录-登录成功"。
4. 步骤要具体可执行,不要写"正常操作"这种模糊描述,要写清楚具体输入什么数据。
5. 优先级判断:涉及核心主流程的定为 P0,一般功能分支定为 P1,细枝末节的辅助验证定为 P2。
6. 不要生成边界值、异常、性能、安全类用例,这些由其他专门的生成流程负责,你只关注正向主流程。
7. 只输出合法 JSON 数组,不要有多余解释文字,不要使用 markdown 代码块包裹。
{% endblock %}

{% block schema %}
{
  "type": "array",
  "items": {
    "type": "object",
    "required": ["title", "precondition", "steps", "case_type", "priority", "covers"],
    "properties": {
      "title": {"type": "string"},
      "precondition": {"type": "string"},
      "steps": {
        "type": "array",
        "items": {
          "type": "object",
          "required": ["step", "expected"],
          "properties": {
            "step": {"type": "string"},
            "expected": {"type": "string"}
          }
        }
      },
      "case_type": {"type": "string", "enum": ["functional"]},
      "priority": {"type": "string", "enum": ["P0", "P1", "P2"]},
      "covers": {"type": "array", "items": {"type": "string"}}
    }
  }
}
{% endblock %}

{% block few_shot %}
### 示例输入
功能点:
- 用户输入正确验证码并点击登录后,可成功登录

### 示例输出
[
  {
    "title": "输入正确手机号和有效验证码登录-登录成功",
    "precondition": "用户已注册,已获取有效期内的验证码",
    "steps": [
      {"step": "在登录页输入已注册手机号13800138000", "expected": "手机号输入框正常显示号码"},
      {"step": "点击'获取验证码'按钮", "expected": "收到6位数字验证码,页面提示发送成功"},
      {"step": "输入收到的正确验证码,点击'登录'按钮", "expected": "登录成功,跳转到首页"}
    ],
    "case_type": "functional",
    "priority": "P0",
    "covers": ["用户输入正确验证码并点击登录后,可成功登录"]
  }
]
{% endblock %}

{% block user %}
### 需求标题
{{ requirement_title }}

### 需求描述
{{ requirement_content }}

### 功能点列表
{% for fp in feature_points %}
- {{ fp }}
{% endfor %}

### 业务规则(供参考,填写步骤时需符合这些规则)
{% for rule in business_rules %}
- {{ rule }}
{% endfor %}

请生成覆盖上述所有功能点的正向功能测试用例,按照 JSON Schema 输出。
{% endblock %}
